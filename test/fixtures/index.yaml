openapi: 3.0.3
info:
    title: My Service
    version: 0.0.0

tags: "@tags.yaml"

components:
    schemas: "@schemas.yaml"

paths:
    /:
        get:
            summary: front page
            responses:
                200:
                    content:
                        text/html:
                            schema:
                                type: string
