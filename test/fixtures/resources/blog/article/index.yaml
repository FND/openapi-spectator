uri: /blog/{slug}
pathParameters:
    slug:
        description: article identifier
        schema:
            type: string

GET:
    summary: show blog post
    queryParameters:
        referrer:
            description: originating URL or service
            schema:
                type: string
