uri: /blog
GET:
    summary: list blog posts
    queryParameters:
        tag:
            description: one or more tags to filter by
            schema:
                type: array
                items:
                    type: string
POST:
    summary: create blog post
